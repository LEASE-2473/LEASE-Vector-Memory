# 项目上下文

## 项目目标

`LEASE Vector Memory` 是独立的 SillyTavern 第三方扩展。它以八张结构化表格维护记忆：七张自动填表详情表与一张仅用户可编辑的“手工记忆”表；每行直接作为向量切片，不再经过 AI 二次总结。

## 技术栈与运行环境

- 原生 JavaScript、jQuery、HTML、CSS。
- SillyTavern 扩展上下文、聊天元数据与隐藏持久化书。
- OpenAI 兼容 Embedding API，可选 Rerank API。
- Node.js 仅用于语法检查和内置行为测试，无生产依赖或构建步骤。

## 核心文件

- `index.js`：v2 表格数据、稳定 R 编号、事务执行、热记忆注入、主界面与请求拦截。
- `memory_lifecycle.js`：行级冷热、锁定、逐表 X、冷记忆书同步及“冷热与 API”中的策略面板。
- `backfill_manager.js`：自动批量填表、手动追溯和来源楼层范围。
- `vector_manager.js`：合并冷热设置的 API 页、统一知识书管理、Embedding、Rerank、检索及 `LEASE_Vector_Memory_Database` 持久化。
- `prompt_manager.js`：追溯提示词和表结构组合方案；手工记忆固定为末表且 AI 不可写。
- `io_manager.js`：v2 JSON/TXT 往返和旧 LEASE Memory Context 导出文件显式迁移。
- `debug_manager.js`：最后请求、插入索引、角色、热行与冷召回诊断。
- `tests/core.test.mjs`：稳定 ID、事务保护、可见性、自动降冷、失败回退和命名空间测试。

## 关键设计决策

- 每张表独立使用稳定的 `R1、R2……`；表号仍为 `0..7`。普通删除、排序与冷热转换不改变或复用 R 编号；用户明确执行整表清空时，该表数据与编号计数器一并销毁，新数据从 R1 重新开始。
- 持久化格式为 v2 `{cells, meta}`，元数据包括 ID、冷热、锁定、来源区间与时间戳。
- 白色热行在日常剧情请求中直接注入；绿色冷行仅语义召回；锁定热行直接注入但对填表 AI 不可见且不可写。
- 批量填表只收到前七表中未锁定的白色行。任何更新/删除指向冷行、锁定行、不存在 ID 或手工记忆时，整批零写入。
- 主线和支线默认自动降冷并保留最近 3 行；其他表默认关闭，预填 3。Embedding 成功后才转绿，失败保持白色。
- 向量区为“冷热与 API / 知识书管理”两页，不重复展示逐行记忆卡片。主表逐行状态只保留转冷/转热与锁定/解锁；R 编号和来源区间拆成两个固定列，来源单行省略显示。关闭向量总开关会恢复所有冷行为白色。
- Embedding 文本不包含 R 编号或来源楼层，这两项只保存在向量条目元数据中；格式升级会使受影响的冷行自动重算，失败行恢复白色。
- 冷记忆书按聊天隔离，条目标识为“表号 + R 编号”；行编辑只使对应文本缓存失效。无时间衰减，默认最多召回 20 条。
- 热表和冷召回默认插在预设/System/世界书之后、历史聊天之前；显式深度设置覆盖默认锚点。
- `summary_manager.js`、总结表、自动总结、总结提示词和“详情→总结→向量”链均已删除。
- 插件 ID 为 `lease_vector_memory`；localStorage、聊天元数据和扩展设置使用 `lvm_`/`lvm` 命名空间，不读取旧插件存储。
- 用户已于 2026-08-16 明确授权公开发布；`origin` 仅指向独立仓库 `https://github.com/LEASE-2473/LEASE-Vector-Memory.git`。禁止向旧 LEASE Fork 或原作者仓库写入。

## 运行与验证

- `npm test`：运行 Node 行为测试。
- `npm run check`：检查核心 JavaScript 语法。
- `node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8'))"`：检查 manifest。
- 实机仍需验证：批量填表、锁定保护、真实 Embedding、冷召回、提示词最终位置、聊天切换与移动端 UI。

## 当前状态与已知问题

- v4.2.6 移除“主线结束时间非空”和“地点变化”的代码级拒绝，允许同一事件弧跨批次延长结束时间并记录多个地点。主线边界改由提示词依据核心目标与因果连续性判断：短距离转场和连续行动优先聚合，核心目标变化、明显长时间跳跃或独立因果链才分行；代码仍严格保护冷行、锁定行、手工记忆和不存在的稳定 R。
- v4.2.5 修复清表未同步重置 `nextRowId` 的根因：所有整表清空路径现会同时将该表编号计数器归 1，而普通删行仍保持不复用。填表提示词不再暴露 `nextRowId`，并恢复以用户提供的 LEASE `新版-backfillPrompt.txt` 完整正文为唯一基线，只机械迁移总结表、数字行索引和冷热/锁定边界，不自行缩写原证据边界、时间轴、七表职责、去重和字段规则。为兼容更新前已清空但计数器已残留的会话，真空表误写任意 update 会转为 insert；非空表仍严格拒绝不存在的 R。
- v4.2.4 修复 v4.2.3 提示词版本迁移覆盖后暴露的空表初始化回归：运行时默认提示词已替换为纯稳定 R 编号的 v4 正文，旧 Base64 提示词不再使用；空表收到旧式 `updateRow(...,"R0",...)` 时仅在安全条件下转为新增 R1。
- v4.2.3 曾加入结束时间与地点变化的主线代码拒绝，已在 v4.2.6 因过度切碎连续剧情而撤销；保留的追加列重复片段与“旧全文＋新增内容”去重保护仍然有效。
- v4.2.2 将来源区间列默认宽度由 118px 缩至 64px，并接入逐表列宽拖动与会话存档；同时修复清表入口残留调用已删除总结管理器的问题，并在清理后同步冷记忆书。版本已发布到独立 GitHub 仓库。
- v4.2.1 已修复来源区间列被通用 `position: relative` 覆盖后相对位移、叠到首个数据列的问题，并为来源文本增加固定宽度裁剪与固定列不透明主题背景。
- 自动降冷面板改为不可收缩卡片，桌面双列、窄屏单列展示八张表；已使用本地浏览器复现页验证 1280px 与 390px 布局。14 项自动测试已通过；真实模型/API 与 SillyTavern 内完整交互仍待回归。
- 为降低一次性重写风险，旧版部分不可达的总结 UI 辅助代码与历史配置字段仍可能存在注释或死分支；总结模块、触发入口、可见配置卡和向量同步入口已移除。后续可继续做纯清理，但不得改变 v2 数据行为。
- 用户显式导入旧文件时会忽略总结表、把旧绿色状态全部转白，再按当前 X 尝试降冷；Embedding 未配置或失败不会丢行。
